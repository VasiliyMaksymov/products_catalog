= form_for Review.new do |f|
  .form-group
    = f.text_area :msg, class: 'feedback form-control'
    = f.hidden_field :user_id, value: current_user.id
    = f.hidden_field :product_id, value: product.id
  .form-group
    = f.button 'Send feedback', class: 'btn btn-success send-feedback'